{
  "name": "ts-issues",
  "version": "0.0",
  "scripts": {
    "test": "yarn jest"
  },
  "dependencies": {
    "jest": "^27.0.5",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "react": "^17.0.1",
    "ts-jest": "^27.0.3",
    "@types/jest": "^26.0.23"
  },
  "peerDependencies": {
    "react": "^16.8.0 || ^17.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "preset": "ts-jest",
    "globals": {
      "ts-jest": {
        "isolatedModules": true
      }
    },
    "roots": [
      "./tests"
    ]
  }
}
